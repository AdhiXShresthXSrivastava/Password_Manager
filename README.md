# 🔐 Python Password Manager (CLI)

A simple **command-line password manager** written in Python.  
It allows you to either **generate strong random passwords** or **save your own passwords** securely into a local file.

---

## ✨ Features
- Generate random strong passwords (letters, digits, symbols).
- Save passwords with a label (e.g., Instagram, Email, GitHub).
- Append new entries without overwriting old ones.
- Simple and beginner-friendly CLI interface.
- Modular functions for easy expansion.

---

## 📂 File Structure
- Main.py
- .venv (if you create it)

---

## 🚀 Usage
Run the script in your terminal:

```bash
python password_manager.py

